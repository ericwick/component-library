import React, { useState, useRef } from 'react';
import className from 'classnames';
import './bento.scss';

function Bento(props) {
  const { active, setActive } = useState(false);

  return <div className="bento-menu"></div>;
}
